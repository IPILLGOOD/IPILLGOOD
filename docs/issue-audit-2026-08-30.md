# GitHub 이슈 전수 점검 — 2026-08-30

## 기준과 판정 원칙

- 기준 브랜치: `main`의 `bf384aa`와 이번 점검 패치
- 열린 이슈 27개를 현재 모델, API, 화면, 테스트, 최근 병합 PR과 대조했다.
- 제목의 핵심 완료조건이 구현된 이슈는 닫고, 뒤에 덧붙은 별도 범위는 중복 이슈로 넘기거나 새 후속 이슈로 분리한다.
- 정책·임상·법률·실사용자 연구는 코드가 있어도 승인 산출물이 없으면 완료로 보지 않는다.
- 부분 구현은 닫지 않고 현재 구현과 남은 경계를 이슈에 기록한다.

## 이슈별 판정

| 이슈 | 판정 | 근거와 조치 |
| --- | --- | --- |
| #47 | 범위 변경 후 완료 | PR #121에서 오늘 화면 인라인 입력을 의도적으로 제거했다. 전용 `/check-in`에서 무응답과 명시적 `not_yet`을 구분하도록 이번 패치에서 보완하고 닫는다. |
| #48 | 유효·차단 | plan revision, 미래 회차 재생성, 정책 #85가 아직 없다. `blocked`로 명시한다. |
| #50 | 이번 패치로 완료 | 빈 상태 안내·로그인 유도에 더해 임의 이름/나이 제거, 서버 온보딩 판정, 직접 URL/API 차단, 저장 후 이동을 보완한다. |
| #52 | 유효·차단 | 실제 질문 응답은 저장되지만 대시보드와 보고서는 여전히 별도 `clinicianQuestions`를 읽는다. #78·#79·#89 이후 통합해야 한다. |
| #53 | 완료 | `e75b715`에서 서울 날짜 최근 7일 필터·빈 기간·실제 증상 집계를 구현했다. 추가된 occurrence/근거 범위는 #78·#79·#89에 이미 있다. |
| #54 | 유효·차단 | `sourceDocumentIds`와 늦은 게시 차단은 있으나 연쇄 삭제·익명화 정책과 재시도는 없다. #62·#88 결정이 필요하다. |
| #55 | 유효·차단 | 공통 삭제 primitive와 회원 탈퇴는 있으나 계정 유지형 건강정보 초기화 UI, 선택적 Auth 삭제, 재인증 흐름이 없다. |
| #56 | 유효 | 중복 등록 방지와 오류 복구는 개선됐지만 서버 job 상태, 취소, 새로고침 후 복구가 없다. |
| #61 | 유효·차단 | 파일 형식 검증은 있으나 민감정보 탐지·전송 확인·비가역 마스킹이 없다. #88 승인이 선행된다. |
| #62 | 유효·차단 | 회원 탈퇴 3개월 만료 삭제는 계정 수명주기일 뿐 데이터 유형별 보관·익명화 정책이 아니다. |
| #66 | 완료 | `61b2f97`에서 claim lease, 429/5xx/timeout 재시도, Retry-After, terminal/accepted 중복 방지를 구현했다. 전달 funnel은 별도 이슈로 분리한다. |
| #67 | 안전 결함 완화·기능 유효 | 이번 패치에서 PRN·주간·모호한 주기와 시각을 매일 09:00으로 만들지 않고 fail-closed한다. 주간·요일·PRN 구조화 지원은 남는다. |
| #72 | 완료 | `998cf98`, `b07aed1`에서 키 비교, 권한 유지 자동 재구독, opt-out 보존, 재진입 검사를 구현했다. |
| #73 | 유효·P0 보안 | 부분 성공 후 숨은 활성 구독과 로그아웃 비활성화 실패의 영구 재시도 경로가 없다. 공유 기기에서 이전 계정 건강 알림이 남을 수 있어 P0로 올린다. |
| #78 | 유효·P0 | 앱 미접속일의 예정 회차 원장이 없다. |
| #79 | 유효·P0 | 현재 체크인은 같은 날 관찰과 정정 이력을 교체하므로 append-only가 아니다. |
| #81 | 유효·차단 | 질문 응답의 urgent 표시 자료는 있으나 승인된 결정 규칙·문구 버전·전 채널 차단이 없다. #86이 선행된다. |
| #82 | 부분 구현·차단 | PR #114에서 단일 코드 연결·세션 검증·철회를 구현했다. 다중 역할, 권한표, 소유권 이전, 감사 로그와 마이그레이션은 남는다. |
| #83 | 유효·차단 | 미복용 이유·정정·후속 할 일 모델이 없다. #78·#79·#86이 선행된다. |
| #84 | 부분 구현·차단 | 인증 화면 인쇄/PDF와 기본 한계 문구는 있으나 채널 ADR, 내보내기 전 범위 확인, 복제본 한계 고지가 없다. |
| #85 | 부분 구현·정책 유효 | draft/review/confirm 기술 경계는 구현됐지만 의료적 권고 경계와 근거 우선순위의 전문가 승인본이 없다. |
| #86 | 유효 | 의료전문가 승인 대응표·문구 버전·재검토 체계가 없다. |
| #87 | 유효 | 재알림·에스컬레이션 정책 결정 이슈이며 아직 승인 산출물이 없다. |
| #88 | 부분 구현·정책 유효 | 동의 서버 게이트와 단일 연결 철회는 구현됐지만 법률 검토 권한표·대리권·외부 AI/공유 문안이 없다. |
| #89 | 부분 구현·정책 유효 | 화면은 “응답 중 복용 완료”, 기간, 상담 보조 한계를 표시하지만 승인된 데이터 사전과 예제 결과가 없다. |
| #90 | 부분 구현·연구 유효 | 자동 접근성 감사 #64는 완료됐지만 고령 당사자·보호자 실사용 연구는 수행되지 않았다. |
| #91 | 유효·차단 | #78·#79·#85·#86·#88·#89·#90과 전달 instrumentation이 미완료다. |

## 보안·개인정보 분류

- `security`: #73, #82, #84, #88
- `privacy`: #50, #54, #55, #61, #62, #73, #82, #84, #88
- 즉시 우선순위 상향: #73을 `[P0]` 및 `release-blocker`로 변경
- 정책 선행으로 차단 표시: #48, #52, #54, #55, #61, #62, #81, #83

## 검증 경계

자동 테스트 통과는 외부 전문가 승인을 대체하지 않는다. 특히 #85·#86·#88·#89, 실제 보조기술/사용자 연구 #90, 실제 Push 기기 수신과 정책 효과 검증 #91은 별도 증거가 필요하다.
